# PROJECT — baristaschool.ru

> **Цель:** системный, рентабельный образовательный бизнес.
> Постепенно обновляем все страницы сайта через Tilda Zero Block.
> Каждая страница работает на конверсию.

> **Последнее обновление:** 11 июня 2026

---

## ✅ Чекпоинты (что уже сделано)

| Дата | Что сделано |
|------|-------------|
| май 2026 | `/prepay` — страница предоплаты, опубликована |
| май 2026 | `/company` — страница «О школе», опубликована |
| май 2026 | `/home_barista_online` — онлайн-курс «Домашний бариста», опубликован |
| май 2026 | Дашборд YClients — аналитика, квартал, услуги, мастер-классы, предоплаты, маркетинг, интеграции |
| май 2026 | **Tilda Back** (`/tilda`) — инструмент управления всеми 56 страницами сайта из дашборда |
| июнь 2026 | `/master_doma` — локальная сборка страницы мастер-класса по сценарию v2: 6 Tilda-блоков, плоская онлайн-запись, плоский блок тренеров, горизонтальная галерея |

---

## 🛠️ Tilda Back — инструмент управления страницами

**URL:** `https://159-194-202-120.sslip.io/tilda`
**Проект Tilda:** ID `1009188` (Schoolbarista)
**API ключи:** хранятся в `.env` (см. `.env.example`). Переменные: `TILDA_PUBLIC_KEY`, `TILDA_SECRET_KEY`

### Что умеет Tilda Back
- **56 страниц** сайта разбиты на 5 групп: Главная / Курсы бариста / Открытие кофейни / Мастер-классы / Сервисные
- Каждая группа сворачивается / разворачивается, состояние запоминается в localStorage
- Поиск по названию и URL в реальном времени
- Фильтры по статусу (todo / wip / done) и приоритету
- Статус и приоритет меняются кликом, сохраняются на сервере
- Прогресс-бар «X из 56 скачано» в шапке + мини-бар в каждой группе
- Кнопка «📥» — скачивает HTML из Tilda API → `data/tilda-html/{pageid}.html`
- Кнопка «📥 Обновить все» — качает все 56 страниц последовательно с прогрессом
- Кнопка «👁 Preview» — модал с вкладками Preview / HTML / Заметки
- Вкладка HTML — кнопка «📋 Скопировать» для быстрой передачи контента в чат
- Ссылки «↗ сайт» (baristaschool.ru) и «✏️ Tilda» (tilda.ru/page) рядом с каждой страницей
- Память фильтра в localStorage при перезагрузке

### Файлы Tilda Back
| Файл | Путь |
|------|------|
| Дашборд HTML | `YClients-Dashboard/public/tilda.html` |
| Список страниц | `YClients-Dashboard/data/tilda-pages.json` |
| Скачанные HTML | `YClients-Dashboard/data/tilda-html/{pageid}.html` (только на сервере) |
| API маршруты | `YClients-Dashboard/server.js` — блок `TILDA BACK` |
| Скрипт CLI | `barista-course/scripts/tilda-fetch.js` |

### API эндпоинты (server.js)
```
GET  /tilda                    → tilda.html
GET  /api/tilda/pages          → список страниц из tilda-pages.json
POST /api/tilda/fetch/:pageid  → скачать HTML из Tilda API, сохранить
GET  /api/tilda/source/:pageid → отдать сохранённый HTML
POST /api/tilda/status         → обновить status / priority / note
```

---

## 🗺️ Карта страниц

Полный список (56 страниц) хранится в `YClients-Dashboard/data/tilda-pages.json`
и управляется через **Tilda Back** (`/tilda`).

### Ключевые страницы — высокий приоритет (ещё не начаты)

| URL | Tilda pageid | Группа |
|-----|-------------|--------|
| `/` — Главная | `20538151` | Главная |
| `/barista_courses` — Курсы бариста (лендинг) | `20539461` | Бариста |
| `/group` — Групповой базовый курс | `20778926` | Бариста |
| `/master_open` — МК «Бюджет кофейни» | `85842646` | Владельцы |
| `/open_coffeeshop` — Курс по открытию | `84318546` | Владельцы |

### Завершённые страницы

| URL | Папка |
|-----|-------|
| `/prepay` | `prepay/` |
| `/company` | `about-school/` |
| `/home_barista_online` | `home-barista-online/` |

---

## 🔄 Рабочий процесс — как работаем со страницами

### Шаг 1 — Получить контент из Tilda
1. Открыть **Tilda Back**: `https://159-194-202-120.sslip.io/tilda`
2. Найти страницу → нажать **📥** (скачается HTML из Tilda API)
3. Нажать **👁 → вкладка HTML → 📋 Скопировать**
4. Вставить HTML в чат — читаю контент, тексты, структуру блоков

### Шаг 2 — Разработка
1. Создать папку `[slug]/` в `barista-course/`
2. Создать `tilda-block.html` по дизайн-системе
3. Создать `index.html` (обёртка для preview)
4. Открыть `index.html` → ПКМ → **Show Preview** для локального просмотра
5. Проверить на мобильном (390px)

### Шаг 3 — Вставка на сайт
1. Перейти в редактор: ссылка **«✏️ Tilda»** из дашборда → `tilda.ru/page/?pageid=...`
2. Добавить Zero Block → «HTML / CSS / JS»
3. Вставить содержимое `tilda-block.html`
4. Обновить статус в Tilda Back (клик по статусу → «Готово»)
5. `git commit` + `git push`

---

## 🔄 Сценарий обновления страницы v2

Использовать для новых страниц мастер-классов и событийных страниц, где нужен быстрый старт по модели `/capping`: контент из Tilda Back, самодостаточные HTML-блоки, локальная проверка, ручная вставка в Tilda.

### 1. Перед началом
1. Выполнить `git status --short --branch`.
2. Убедиться, что незавершённые изменения в соседних страницах не относятся к задаче.
3. Прочитать:
   - `PROJECT_MAP.md`;
   - `PROJECT_STATE.md` нужной страницы, если он уже есть;
   - `/Users/Romka/Downloads/All_Code/mbs-design-system/DESIGN_SYSTEM.md`;
   - `/Users/Romka/Downloads/All_Code/mbs-design-system/YCLIENTS_SYNC.md`, если есть расписание или запись.

### 2. Забрать текущий контент
1. Открыть Tilda Back: `https://159-194-202-120.sslip.io/tilda`.
2. Найти страницу, скачать свежий HTML через кнопку `📥`.
3. Открыть Preview → вкладка HTML → скопировать HTML.
4. Зафиксировать в рабочем описании:
   - production URL;
   - Tilda pageid;
   - ссылку на редактор Tilda;
   - текущие важные блоки, попапы, формы, отзывы, оплату или запись.

### 3. Определить механику действия
Перед вёрсткой выбрать один источник записи/оплаты:

| Тип | Что делать |
|---|---|
| yClients | Сверить `service_id`, endpoint и fallback по `YCLIENTS_SYNC.md`; проверять `booking_url` allowlist'ом yClients |
| Tilda Shop | Оставить нативные ссылки `#order:...`; не добавлять JS-запись |
| Hosted widget | Не переписывать backend; встроить текущий HTML/CSS/JS виджета или loader |
| Статичная кнопка | Использовать `#consalt`, `#waiting_list`, Telegram или WhatsApp по дизайн-системе |

Если механика не ясна, сначала описать риск и уточнить, не писать новую запись “на глаз”.

### 4. Создать папку по шаблону
1. Скопировать `_templates/tilda-event-page/` в новую папку страницы.
2. Переименовать `PROJECT_STATE.template.md` в `PROJECT_STATE.md`.
3. Заполнить паспорт страницы: URL, pageid, источник контента, тип записи/оплаты, Tilda-попапы, запреты.
4. Собирать Tilda-блоки в порядке:
   - `00-seo-and-page-styles.html`;
   - `01-top-content.html`;
   - `02-schedule-widget-styles.html`;
   - `03-schedule-widget-script.html`;
   - `04-bottom-content.html`;
   - `05-page-scripts.html`.

### 5. Проверить перед Tilda
Минимальный набор:
- каждый HTML-блок заметно меньше лимита Tilda;
- JSON-LD парсится;
- обычные `<script>` проходят `new Function(...)`;
- на странице один H1;
- все изображения с `alt`;
- canonical/OG/Twitter/meta соответствуют production URL;
- FAQ на странице совпадает с `FAQPage`;
- mobile 390px без наложений, нечитаемых кнопок и растянутых фото;
- Mulish не перебивается Tilda;
- `#consalt`, `#waiting_list`, `#order:...` не конфликтуют с видимыми `id`;
- запись или покупка работает по текущей механике страницы.

### 6. Вставить и сохранить
1. В Tilda вставить блоки строго по порядку `00 → 05`.
2. Не публиковать и не использовать Tilda API без отдельного запроса.
3. После проверки обновить `PROJECT_STATE.md`, `PROJECT_MAP.md` и при необходимости дизайн-систему.
4. Перед GitHub stage только файлы текущей страницы и связанные базы знаний.
5. Делать отдельный commit/push, не захватывая чужие незавершённые изменения.

---

## ⚡ Как использовать PROJECT.md + Tilda Back для максимальной эффективности

### Как начинать каждую сессию с Copilot

Достаточно написать одно из:

```
Хочу сделать страницу /barista_courses
```
```
Продолжаем работу по PROJECT.md — что делаем дальше?
```
```
Скачай HTML страницы 20539461 и сделай из неё tilda-block
```

Copilot читает PROJECT.md → понимает стек, дизайн-систему, стандарт папки → не нужно объяснять контекст заново.

---

### Цикл работы над одной страницей (шпаргалка)

```
1. Tilda Back (/tilda)
   → найти страницу → 📥 скачать HTML → 👁 Preview → вкладка HTML → 📋 Скопировать

2. Вставить в чат:
   "Вот HTML страницы /barista_courses — сделай tilda-block по дизайн-системе"

3. Copilot создаёт:
   barista-courses/
     tilda-block.html   ← вставить в Zero Block на Tilda
     index.html         ← обёртка для preview
     PROJECT_STATE.md

4. Открыть `index.html` → ПКМ → **Show Preview** для локального просмотра

5. Проверить на мобильном → вставить в Tilda Zero Block

6. В Tilda Back: статус → ✅ Готово
```

---

### Как Tilda Back ускоряет работу

| Функция | Как использовать |
|---------|-----------------|
| **Фильтр "todo + high"** | Открывает очередь: что делать прямо сейчас |
| **Кнопка 📥** | Получить свежий HTML прямо из Tilda API одним кликом |
| **Вкладка HTML → 📋** | Скопировать контент и вставить в чат Copilot |
| **Вкладка Заметки** | Записать, что нужно сохранить или изменить на странице |
| **📥 Обновить все** | Перед началом спринта скачать все 56 страниц разом |
| **Ссылка ✏️ Tilda** | Сразу открыть редактор нужной страницы |

---

### Типовые команды для Copilot (copy-paste)

**Начать новую страницу:**
```
Скачай HTML страницы [pageid] через Tilda Back.
Вот содержимое: [вставить скопированный HTML].
Создай папку [slug]/ по стандарту PROJECT.md. Дизайн-система в DESIGN_SYSTEM.md.
```

**Обновить существующую страницу:**
```
Вот обновлённый HTML из Tilda (страница /[slug]).
Обнови tilda-block.html — тексты и структура изменились, стили сохрани.
```

**Проверить статус работ:**
```
По PROJECT.md — какие страницы high priority ещё не начаты?
```

---

### Приоритеты (что делать в первую очередь)

Смотреть через Tilda Back фильтр `high + todo`:

| Страница | pageid | Почему важна |
|----------|--------|-------------|
| `/` — Главная | `20538151` | Точка входа 100% трафика |
| `/barista_courses` — Курсы (лендинг) | `20539461` | Главный продуктовый лендинг |
| `/group` — Групповой курс | `20778926` | Ключевой продукт |
| `/open_coffeeshop` — Открытие кофейни | `84318546` | Второй по объёму продукт |

---

## 🏗️ Стандарт папки страницы

Для новых событийных страниц и страниц мастер-классов использовать шаблон:

```
_templates/tilda-event-page/
```

Он даёт разбиение на Tilda-блоки `00 → 05`, локальное превью и паспорт страницы.

Базовая структура новой событийной страницы:

```
page-slug/
  tilda-blocks/
    00-seo-and-page-styles.html
    01-top-content.html
    02-schedule-widget-styles.html
    03-schedule-widget-script.html
    04-bottom-content.html
    05-page-scripts.html
    README.md
  index.html
  PROJECT_STATE.md
  tilda-block.html   ← полный локальный источник/архив, если нужен
```

Для старых страниц с одним блоком сохраняется прежний стандарт:

```
page-slug/
  tilda-block.html   ← ЕДИНСТВЕННЫЙ рабочий файл (вставляется в Tilda Zero Block)
  index.html         ← локальная обёртка для просмотра в браузере (ПКМ → Show Preview)
  PROJECT_STATE.md   ← актуальное состояние именно этой страницы
```

**Именование папок:** совпадает с URL-слагом страницы на сайте.

---

## 🎨 Дизайн-система

Полный гайд: `/Users/Romka/Downloads/All_Code/mbs-design-system/DESIGN_SYSTEM.md`

### Быстрая шпаргалка

**Шрифт:** Mulish (400–900), Google Fonts
**Иконки:** Phosphor Icons (`https://unpkg.com/@phosphor-icons/web@2.1.1`)
**Префикс классов:** `mbs-[блок]__[элемент]`

**Цвета:**
```
#417033  — зелёный тёмный (лейблы, акценты)
#4F883E  — зелёный (кнопки, hover)
#E7F2E3  — фон зелёный светлый (лейблы, CTA-карточка)
#CC2841  — красный (главная кнопка CTA)
#1F1F1F  — чёрный (заголовки, текст)
#555555  — серый (второстепенный текст)
#F5F5F5  — фон серых блоков
#FFFFFF  — фон белых блоков
```

**Ширина контента:** `max-width: 1100px; margin: 0 auto;`
**Отступы блоков:** `padding: 90px 20px` (mobile: `64px 14px`)

**Чередование фонов блоков:**
```
Hero        → белый снаружи + #F7F7F7 карточка
For-whom    → #F5F5F5
Skills      → #FFFFFF
How-it      → #E7F2E3
Program     → #FFFFFF
Price       → #F5F5F5
FAQ         → #F5F5F5
Final CTA   → белый снаружи + #E7F2E3 карточка
```

---

## ⚙️ Технология вставки в Tilda

- Метод: **Zero Block** → «HTML / CSS / JS» на каждой странице
- Файл вставляется в DOM страницы Tilda напрямую (не iframe)
- Tilda добавляет свои шрифт/стили — наши классы с префиксом `mbs-` не конфликтуют
- Для каждого элемента используем `box-sizing: border-box`
- **НЕ используем** глобальные селекторы (`body`, `*`, `html`) — только через `.mbs-БЛОК *`

**Tilda Shop (кнопки оплаты):**
- Нативные `<a href="#order:Название = сумма:::image=URL">` — Tilda слушает сама
- Никакого JS для корзины не нужно

---

## 👁️ Предпросмотр страниц (Live Preview)

Для просмотра страниц используем встроенный **Live Preview** VS Code:

1. Открыть `index.html` нужной страницы в проводнике
2. ПКМ → **Show Preview** (или кнопка 🔍 в заголовке вкладки)
3. Проверить на мобильном: DevTools → Toggle Device Toolbar → 390px

**На боевой сайт** — вставляем `tilda-block.html` вручную в Zero Block на Tilda после проверки.

---

## 🖥️ Инфраструктура (сервер)

| Компонент | Значение |
|-----------|---------|
| Сервер | `root@159.194.202.120` |
| HTTPS (sslip) | `https://159-194-202-120.sslip.io/` |
| Боевой домен API | `https://api.barista-school.ru/` |
| SSH ключ | `~/.ssh/copilot_beget_temp/id_ed25519` |
| Путь на сервере | `/opt/yclients-dashboard/` |
| PM2 процесс | `yclients-dashboard` (порт 3010) |
| Деплой | `scp` файлов + `pm2 restart yclients-dashboard --update-env` |

---

## 🧠 Принципы конверсии (применяем везде)

- **Один главный CTA** на экране — красная кнопка `#CC2841`
- **Социальное доказательство** рядом с CTA (отзывы, цифры)
- **Устранение возражений** до кнопки, не после
- **Мобиль первичен** — проверяем на 390px
- **Скорость** — никаких тяжёлых библиотек, только то что нужно
- **Ясность** — заголовок отвечает на вопрос «что я получу»

---

## 📁 Структура репозитория

```
barista-course/
  PROJECT.md                    ← этот файл (главная база знаний)
  scripts/
    tilda-fetch.js              ← CLI скрипт: список страниц / скачать по pageid
  prepay/                       ← /prepay ✅
  about-school/                 ← /company ✅
  home-barista-online/          ← /home_barista_online ✅
  barista-theory-cabinet/       ← личный кабинет учеников
  certificates/                 ← сертификаты
  [новые папки по мере работы]

YClients-Dashboard/             ← бэкенд дашборд (отдельный проект, сервер)
  server.js                     ← Express API, порт 3010
  public/
    tilda.html                  ← Tilda Back дашборд (/tilda)
    index.html / analytics / marketing / ...
  data/
    tilda-pages.json            ← 56 страниц с метаданными и группами
    tilda-html/                 ← скачанные HTML страниц (только на сервере)
  src/
    morning-digest.js           ← вечерний дайджест в Telegram
```
